# homebrew-stackimport

Homebrew tap for `stackimport`.

Stable releases from `jrepp/stackimport` update this tap automatically after the
GitHub release is published successfully.

```sh
brew tap jrepp/stackimport
brew install stackimport
```

Until the first release containing tap automation is published, use the source
repository directly for local builds.
